#include "scanner.h"

// Usar función isspace().
// Usar función ungetc().

/**
 * @brief -
 * 
 * @return token 
 */

Token get_token(char *buffer){

    int cursor;
    char lectura = getchar();
    lectura = getchar();
    

    return 0;
}